# Draft reply to Jo — 2026-08-18 (v4, condensed)

**Re:** T2026C — all six outputs attached, six clashes to check

---

Dear Jo,

Thank you — that answered everything. All six tasks are done. Attached:

- `Classes_DayTimeLocation_T2026C.xlsx` — Task 1. Same row order as your `Classes` sheet, so it pastes straight into K:M. The `_Issues` tab lists anything I had to interpret.
- `DailyTimetable_T2026C.xlsx` — Tasks 2 and 4. 4,494 rows; the English rows already change lecturer every five lessons.
- `EnglishNetAssignment_T2026C.xlsx` — Task 3, with the loading table.
- `DAE_TeachingSchedule_T2026C.xlsx` — Task 5, your ten staff.
- `ClassTimetable_T2026C.xlsx` — Task 6, all 32 classes.

**1. Six clashes, please check**

I filtered out combine classes (same room, same teacher). These are left — all
cells below are in `T2026C_WeeklyTimetable_V4 0817.xlsx`:

| Cell | Clash |
|---|---|
| `Tue(Term1)` **A28** | Classroom blank. `DAE101_ST1` (Mercury Lee) vs `DAE102_FL1` (Elise Ye) at ST-ST3 |
| `Tue(Term1)` **A21** | Classroom blank. `DAE106_TW2` (Newman Lo) vs `DAE102_TW1` (Ray Leung) at TW-TW1 |
| `Fri(Term1)` **D33, D30** | Philip Chan 0900–1300: `DAE205_A` @SSP-303 vs `DAE229_A` @SSP-201 |
| `Fri(Term1)` **D34, D30** | Philip Chan 0900–1300: `DAE205_B` @SSP-303 vs `DAE229_A` @SSP-201 |
| `Wed(Term1)` **T30, T32** | Philip Chan 1400–1800: `DAE243_C` @SSP-104 vs `DAE243_A` @SSP-201 |
| `Tue(Term1)` **AA5, V4** | CS5 students: `DAE103_CS5` 1600–1800 vs `DAE108_CS5` 1400–1800 |

The first two are blank classroom cells where I took the room from the row above,
but the neighbour has a different subject and teacher — so probably not combine
classes. Likely just a missing room.

The `_Issues` tab of `Classes_DayTimeLocation_T2026C.xlsx` has the same list with
every cell reference, sorted so anything needing your decision is at the top and
highlighted in yellow.

**2. Net hours — I used 720 for Ms. Kaur**

Your figures left a 40-hour gap (1,080 needed against 1,040). Your first email
gave Ms. Kaur a range of 640–720, so I took the top, which closes it exactly.
Please say if that is not acceptable.

It works, and every contracted figure lands exactly — Kaur 720, Wong 660, Ip 420,
Barrett 360, Yuen 200 across CS1 and CS2. But there is zero slack: your 28
English classes sit in only six day-and-time slots, so Ms. Kaur must teach one
class in every slot in every five-week block. **If any English class moves day or
time, the Net arrangement has to be recalculated and may not be possible.**

**3. Three quick questions**

- Teacher view: your template uses a different style on every sheet, so I
  standardised on Kevin's — subject code, then centre in brackets. Alright?
- Your template has no sheet for Ms. Kaur or Ms. Wong, so I added them. It does
  have Mike, Chris Hon and Mercury (PT), who are not on your list of ten — leave
  them out?
- Saturday classes (`DAE260_A`–`_F`) have 14 lesson days, not 15, because lesson
  15 is still TBC — cells **AL18** and **AM18** on `DAE Calendar 2026-2027`.
  Send me the date and I will fill those rows in.

`DAE256_G/H`, `DAE258_G/H`, `DAE260_G/H` and `DAE270` are excluded as agreed —
once the police college file and AASFP confirmation arrive I can regenerate
everything in a few minutes.

Best regards,
Steven
